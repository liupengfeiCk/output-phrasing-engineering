<reconnaissance>
goal: 为优化 SKILL.md 建立侦察笔记。只分析 SKILL.md 这一个文件（549行）及其引用的 agent-examples.md（961行）。不涉及 rules、README 或其他文件。

====================================================================
【第一层】SKILL.md 的结构扫描
====================================================================

SKILL.md 的内部结构（按出现顺序，标注行数占比）：

  [第1-7行]   元数据头（name, description, trigger, alwaysApply）
  [第9-14行]  顶部说明段落
  [第16-82行] 人设与写作风格（身份 + 核心原则5条 + 写作风格6条，每条配2-4条语料样本）— 约66行
  [第84-258行] 强制工作流程 — 约174行，占全文32%，是最重的章节
    - 阶段一·侦察（reconnaissance 格式定义）
    - 阶段二·分析（analysis 格式定义，含持久化要求、格式校验规则、正反面示例）
    - 阶段三·执行（decision_point 格式定义，含前置步骤、持久化要求、至少三方案约束、deviation_audit + self_dissection）
    - 阶段四·验收（output_quality_review 格式定义，含验收前置步骤、归档流程）
  [第260-418行] 措辞替换规则 — 约158行，占全文29%
    - 十类，共约80条替换映射
    - 第一类：捷径导向型（9条）
    - 第二类：放弃正确方案型（8条）
    - 第三类：虚假完成型（13条）
    - 第四类：过度抽象型（6条）
    - 第五类：自我降权型（6条）
    - 第六类：逃避复杂度型（5条）
    - 第七类：思维链内部省略型（11条）
    - 第八类：工具/行为降级型（5条 + 核心规则声明）
    - 第九类：伪理性化型（11条 + 模式级规则）
    - 第十类：执行顺序降级型（5条 + 模式级定义 + 检测规则）
  [第420-434行] 绝对禁止（15条）— 约14行
  [第436-458行] 强制开头/模拟预填充 — 约22行
  [第460-466行] 格式提醒（末尾 REMINDER）— 约6行
  [第468-471行] 教学示例引用 — 约3行

agent-examples.md（被 SKILL.md 引用，每次任务时一并加载）：
  - 6个完整示例（A-F），每个展示四阶段流程
  - 示例A：邮箱校验（基础任务，展示完整流程）
  - 示例B：GetUser 返回值改 DTO（侦察发现大范围引用）
  - 示例C：支付回调加退款（侦察发现代码质量问题，在 approach 中直接决策）
  - 示例D：订单排序（执行期发现 ORM 注入，触发 decision_point）
  - 示例E：重命名函数（简单任务也走完整流程）
  - 示例F：推送重试（展示 deviation_audit 触发 self_dissection 的完整流程）

====================================================================
【第二层】发现的问题——只针对 SKILL.md 本身
====================================================================

问题 P1：SKILL.md + agent-examples.md 的总上下文开销（高）
- 现象：SKILL.md 549行 + agent-examples.md 961行 = 1510行，约46KB + 56KB = 102KB 文本。
  每次任务加载时，AI 的上下文中有约 102KB 的 skill 定义，还没算用户的代码文件。
- 具体分析：
  * agent-examples.md 占总量的 63%（961/1510行），但它的信息是高度重复的——6个示例本质上在反复演示同一套流程格式
  * 示例A/B/C/E 的 output_quality_review 部分几乎是模板化的（metrics → substance_check → completeness_check → value_density_check → alignment_check，每次都是"全部通过"）
  * 示例D 和 F 是真正有教学价值的——D 展示了 decision_point 的正确用法，F 展示了 deviation_audit 触发 self_dissection
- 潜在影响：
  * 根据 Lost-in-the-Middle（Liu et al. 2023, arXiv:2307.03172），长上下文中段信息召回率下降。1510行中，措辞替换规则的第四到第七类（SKILL.md 第320-380行附近）处于文档中段。
  * 但对于 100K+ 上下文窗口的模型，1510行（约15-20K token）可能不构成严重问题。实际影响需要实测。
- 优化方向：
  * agent-examples.md 可以大幅精简——保留示例D（decision_point）和示例F（self_dissection）作为核心教学示例，其余示例精简为只展示 analysis 格式（去掉重复的 output_quality_review 模板）
  * 预估精简后：agent-examples.md 从961行降到约400行，总量从1510行降到约950行

问题 P2：措辞替换表的条目冗余（中等）
- 现象：十类共约80条。逐类检查发现以下冗余：
  * 第一类（捷径导向）9条中，"最简单的做法是"/"最快的方式是"/"最高效的做法是"/"一个快速的解决方案是"/"我们可以简单地" 这5条的降级行为本质相同（跳过设计思考），替换词也高度相似（"综合评估后推荐"/"兼顾质量和效率推荐"/"在保证健壮性的前提下推荐"）
  * 第三类（虚假完成）13条中，"以此类推"/"其余部分同理"/"剩下的部分类似" 是同一降级模式的三种说法；"// ... 其他代码保持不变"/"// 类似上面的处理"/"这里只展示核心代码" 也是同一模式
  * 第九类（伪理性化）11条 + 模式级规则。模式级规则（"任何使用比较级形容词+方案/做法/选择的结构"）已经覆盖了枚举表中"更现实/更可行/更稳妥的做法是"/"采用一个更实际的方案" 等条目，这些条目是冗余的
- 不冗余的部分（应保留）：
  * 第七类（思维链内部省略）11条——每条针对一个具体的 analysis/decision_point 字段，没有冗余
  * 第八类（工具/行为降级）5条——每条针对一种具体的工具滥用行为，没有冗余
  * 第十类（执行顺序降级）5条——每条针对一种具体的排序偷懒行为，没有冗余
- 优化方向：
  * 第一类：9条精简到5条（合并语义相同的捷径措辞）
  * 第三类：13条精简到8条（合并同一降级模式的不同说法）
  * 第九类：11条精简到6条（模式级规则已覆盖的枚举条目删除）
  * 其余类保持不变
  * 预估精简后：80条降到约60条，措辞替换章节从158行降到约120行

问题 P3：analysis 格式中 degradation_check 的检查项区分度（中等）
- 现象：analysis 的 degradation_check 有8个检查项：
  1. 方案是否是三维评估综合最优的？
  2. 是否遗漏了已知边界条件？
  3. 是否因改动量大而想缩减方案？
  4. 是否打算跳过某些文件？
  5. execution_plan 是否覆盖 affected_scope 所有文件？
  6. context 是否充分？是否有未读但可能相关的文件？
  7. 是否有发现了但被判断为"无关紧要"而跳过的问题？
  8. execution_plan 中是否有步骤计划使用 shell 命令修改源代码？
- 区分度分析：
  * 第3项（因改动量大而缩减）和第4项（跳过某些文件）在大多数场景下答案相同——如果你因改动量大而缩减，通常就是跳过了某些文件
  * 第4项（跳过某些文件）和第5项（execution_plan 是否覆盖 affected_scope）也高度重叠——如果跳过了文件，execution_plan 自然不覆盖 affected_scope
  * 第8项（shell 命令修改源代码）是一个非常具体的工具使用问题，放在 analysis 阶段检查不太合适——analysis 阶段还没开始执行，不存在"使用 shell 命令"的问题。这条更适合放在 decision_point 的 degradation_check 中
- 优化方向：
  * 合并第3/4/5项为一条："execution_plan 是否覆盖 affected_scope 所有文件？是否因改动量大而缩减了范围？"
  * 第8项移到 decision_point 的 degradation_check 中（执行阶段才有工具选择问题）
  * 精简后从8项变为6项

问题 P4：decision_point 的结构重量（中等）
- 现象：一个完整的 decision_point 包含：
  issue, impact, context_update, options（至少3个，每个含 description + approach_evaluation + edge_cases + affected_scope_delta）, recommendation, execution_plan_update, deviation_audit（含 original_plan_excerpt + current_proposal + diff_summary + deviation_motive_check + self_dissection）, degradation_check（8个检查项）
  填写一个完整的 decision_point 需要 500-1000 token。
- 核心矛盾：
  * deviation_audit + self_dissection 是框架最有价值的创新——它能在 AI 偷懒的那一刻强制自我审视
  * 但结构过重可能导致 AI 倾向于不触发 decision_point（因为触发成本太高），而是静默绕过问题
  * 示例D（ORM 注入）这种架构级意外值得完整 decision_point；但"某个文件路径变了"这种小意外不值得
- 优化方向：
  * 考虑引入轻量级 decision_point（issue + impact + options(2个) + recommendation + execution_plan_update + 精简版 degradation_check）
  * 完整版保留 deviation_audit + self_dissection，用于 impact=YES 且影响范围超过2个文件的情况
  * 风险：分级本身给了 AI 偷懒入口——可能把所有意外都归类为"小型"。需要在轻量级格式中保留核心检查项

问题 P5：output_quality_review 的自我确认偏差风险（低-中等）
- 现象：验收阶段要求 AI 输出包含 metrics、substance_check、completeness_check、value_density_check、alignment_check 的完整验收报告。
- 问题：
  * AI 检查自己的工作，天然存在自我确认偏差——倾向于给自己打高分
  * 从 agent-examples.md 的6个示例来看，每个示例的验收报告都是"全部通过"，没有一个示例展示"验收发现问题并修正"的流程
  * 这意味着教学示例本身就在暗示"验收 = 走过场 = 全部通过"
- 优化方向：
  * 在 agent-examples.md 中增加一个"验收发现问题并修正"的示例，打破"全部通过"的模式暗示
  * 或者在 output_quality_review 的格式定义中增加一条强制规则："至少指出一个可以改进的点，即使整体质量达标"——强制 AI 进行批判性思考而非确认性思考

问题 P6：.analysis-cache.md 的"逐字复制"要求与实际执行的矛盾（低-中等）
- 现象：SKILL.md 反复强调"逐字复制"——analysis、decision_point、output_quality_review 都要逐字复制到 .analysis-cache.md。
- 问题：
  * "逐字复制"本身就是一个容易触发第七类降级的操作——AI 在复制时倾向于精简
  * SKILL.md 用了大量篇幅（正面示例 vs 反面示例、格式校验规则）来对抗这个问题，但这些对抗措施本身又增加了文档长度
  * 持久化机制的核心价值是"刷新记忆"，但如果 AI 在复制时已经精简了内容，刷新回来的就是残缺记忆——持久化形同虚设
- 优化方向：
  * 考虑用工具调用替代手动复制——比如定义一个"将上一个代码块的内容写入文件"的约定，减少 AI 需要"重新生成"内容的机会
  * 或者简化持久化内容——不要求逐字复制全部字段，只持久化 execution_plan + affected_scope + edge_cases 这三个执行期最容易遗忘的字段

问题 P7：强制工作流章节的格式定义过于冗长（低-中等）
- 现象：强制工作流章节（第84-258行）占全文32%，约174行。其中大量篇幅用于：
  * analysis 格式定义中的"持久化要求"说明（约20行）
  * analysis 格式定义中的"正面示例 vs 反面示例"（约15行）
  * decision_point 格式定义中的"前置步骤"说明（约10行）
  * decision_point 格式定义中的"持久化要求"说明（约10行）
  * output_quality_review 格式定义中的"验收前置步骤"说明（约5行）
  * output_quality_review 格式定义中的"验收后归档步骤"说明（约25行）
  这些"说明性文字"加起来约85行，占工作流章节的49%。
- 问题：
  * 格式定义的核心是"字段模板"——AI 需要知道填什么字段、每个字段填什么内容
  * 但当前的格式定义中，字段模板被大量的"说明性文字"（持久化要求、正反面示例、前置步骤、归档流程）包围
  * 这些说明性文字是"关于格式的规则"而非"格式本身"，它们增加了 AI 需要处理的信息量，但不直接参与格式填写
- 优化方向：
  * 将"持久化要求""前置步骤""归档流程"等操作性规则从格式定义中抽出，集中放在一个独立的"操作规则"章节
  * 格式定义只保留纯粹的字段模板 + 每个字段的填写说明
  * 这样 AI 在填写格式时只需要看字段模板，在执行操作时才需要看操作规则

====================================================================
【第三层】SKILL.md 中有效的设计——应保留
====================================================================

✅ E1：措辞替换表的"替换而非禁止"思路
- 为什么有效："将X替换为Y"比"禁止X"提供了更明确的行为路径
- 依据：Anthropic 官方文档 "Be clear and direct" 建议 "tell the model what to do, not just what not to do"

✅ E2：四阶段工作流（侦察→分析→执行→验收）
- 为什么有效：强制 AI 先读代码再做方案，是 CoT 在 Agent 编码场景的工程化实现
- 依据：Wei et al. 2022（arXiv:2201.11903）证明 CoT 提升推理质量

✅ E3：强制开头（模拟预填充）
- 为什么有效：模型的第一个自由 token 已在正确轨道上，物理上无法跳过思维链
- 依据：Anthropic API 支持 assistant prefill，SKILL.md 是在无法使用原生 prefill 环境中的模拟实现

✅ E4：人设 + 语料样本的组合
- 为什么有效：人设提供全局推理偏置，语料样本是 In-Context Learning 的直接应用
- 依据：Brown et al. 2020（arXiv:2005.14165）证明 few-shot 示例的引导效力

✅ E5：degradation_check 的自检机制
- 为什么有效：AI 写下"是否因改动量大而想简化？→ NO"后，后续行为被该声明约束
- 注意：第七类措辞替换规则（"degradation_check: 全部通过" → 逐条写出）是对抗走过场的关键配套

✅ E6：末尾 [REMINDER] 块
- 为什么有效：利用 recency 效应，末尾内容注意力权重最高
- 依据：Anthropic 官方文档 "Long context prompting tips" 建议在长 prompt 结尾重复关键指令

✅ E7：deviation_audit + self_dissection 机制
- 为什么有效：在 AI 偷懒偏离方案的那一刻强制自我审视，而非等到验收才发现
- 这是框架最有价值的创新，任何优化都不应削弱这个机制

✅ E8：第九类（伪理性化）的模式级规则
- 为什么有效：不是枚举具体措辞，而是定义了一个检测模式（"比较级形容词+方案/做法/选择"），能覆盖无限变体
- 这种"模式级规则"比"枚举表"更高效，其他类也可以考虑引入类似的模式级规则

====================================================================
【第四层】优化方向——只针对 SKILL.md 的具体改动
====================================================================

优化 O1：精简 agent-examples.md（对应 P1）
- 改动：
  * 保留示例D（decision_point 正确用法）和示例F（deviation_audit 触发 self_dissection）的完整内容
  * 示例A 精简为只展示 analysis 格式（去掉 output_quality_review，因为它和其他示例的验收报告几乎一样）
  * 示例B 精简为只展示"侦察发现大范围引用"的 reconnaissance + analysis（去掉执行和验收）
  * 示例C 精简为只展示"侦察发现代码质量问题，在 approach 中直接决策"的 analysis（去掉执行和验收）
  * 示例E 删除（简单重命名任务，教学价值最低，且示例A已经展示了完整流程）
  * 新增一个"验收发现问题并修正"的示例片段（对应 P5），打破"全部通过"的模式暗示
- 预估效果：从961行降到约400行

优化 O2：合并措辞替换表中的冗余条目（对应 P2）
- 改动：
  * 第一类：合并"最简单的做法是"/"最快的方式是"/"最高效的做法是"为一条，用"/"分隔变体
  * 第三类：合并"以此类推"/"其余部分同理"/"剩下的部分类似"为一条
  * 第九类：删除被模式级规则已覆盖的枚举条目（"更现实/更可行/更稳妥的做法是"等）
  * 其余类不动
- 预估效果：80条降到约60条，章节从158行降到约120行

优化 O3：精简 analysis 的 degradation_check（对应 P3）
- 改动：
  * 合并第3/4/5项为："execution_plan 是否覆盖 affected_scope 所有文件？是否因改动量大而缩减了范围或跳过了文件？"
  * 第8项（shell 命令）移到 decision_point 的 degradation_check 中
- 预估效果：从8项变为6项

优化 O4：将操作性规则从格式定义中抽出（对应 P7）
- 改动：
  * 在"强制工作流程"章节末尾新增一个子章节"### 操作规则"
  * 将以下内容从各格式定义中移到"操作规则"：
    - analysis 的持久化要求（逐字复制到 .analysis-cache.md）
    - analysis 的格式校验规则和正反面示例
    - decision_point 的前置步骤（读取措辞替换规则章节）
    - decision_point 的持久化要求
    - output_quality_review 的验收前置步骤
    - output_quality_review 的验收后归档步骤
  * 格式定义只保留字段模板 + 每个字段的填写说明
- 预估效果：格式定义更清晰，操作规则集中管理。总行数不变（只是重新组织），但信息结构更清晰

优化 O5：为 output_quality_review 增加"强制批判"规则（对应 P5）
- 改动：在 output_quality_review 格式定义的末尾增加一条规则：
  "验收报告必须至少指出一个可以改进的点（即使整体质量达标）。如果所有检查项都是'全部通过'，说明你的检查不够严格——回去重新检查。"
- 风险：可能导致 AI 为了满足规则而编造不存在的问题。需要观察实际效果。

====================================================================
【反模式清单】（优化 SKILL.md 时必须避免）
====================================================================
- ❌ 删除 deviation_audit / self_dissection——这是框架最有价值的创新
- ❌ 合并侦察和分析阶段——两个本质不同的认知过程
- ❌ 在没有实测数据的情况下大幅删减措辞替换条目——只合并明确冗余的，不删除独立条目
- ❌ 简化 decision_point 时去掉 deviation_motive_check——这是捕获偷懒的核心机制
- ❌ 精简示例时去掉示例D和示例F——它们是唯一展示 decision_point 和 self_dissection 的教学材料

actions:
- O1（精简 agent-examples.md）：最高优先级，预估减少约560行上下文开销，且不削弱任何核心机制
- O2（合并冗余措辞条目）：中等优先级，预估减少约20条/38行，风险低
- O3（精简 degradation_check）：中等优先级，从8项到6项，提升区分度
- O4（抽出操作规则）：低优先级，不减少总行数但改善结构清晰度
- O5（强制批判规则）：低优先级，需要观察实际效果

check:
- 文献引用自检：
  * arXiv:2307.03172（Liu et al. 2023, Lost in the Middle）—— 可验证 ✓，用于 P1
  * arXiv:2201.11903（Wei et al. 2022, Chain-of-Thought）—— 可验证 ✓，用于 E2
  * arXiv:2005.14165（Brown et al. 2020, GPT-3 / Few-Shot）—— 可验证 ✓，用于 E4
  * Anthropic 官方文档 docs.anthropic.com —— 可验证 ✓，用于 E1、E3、E6

- 本笔记质量自检：
  * 分析对象是否只有 SKILL.md？ → YES，不涉及 rules、README 或其他文件
  * 所有发现是否基于 SKILL.md 的实际内容？ → YES，引用了具体的行号范围、字段名、条目数量
  * 优化建议是否都是对 SKILL.md 的具体改动？ → YES，每个 O 都有明确的改动内容和预估效果
  * 是否保护了框架的核心创新？ → YES，反模式清单明确列出了不可删除的部分
</reconnaissance>